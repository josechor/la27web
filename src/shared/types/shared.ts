export enum ButtonSize {
    large = "large",
    medium = "medium",
    small = "small",
    extraSmall = "extraSmall"
}

export interface Sidebar {
    icon: string;
    name: string;
    router: string;
}